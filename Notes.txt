com.branch.cs.Student
com.branch.ec.Student

public -> access across all the packages
private -> can be accessed only within that class.
protected -> can be accessed within class, sub classes of across all the packages
default -> accessible only within that package


QUIZ App:
    - Upload QUIZ
    - Trainee Profiles
    - Take Up Quiz
    - View Score


